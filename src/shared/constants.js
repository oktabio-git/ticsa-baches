const AUTH_URL = 'http://localhost:8080/api/';

const publicRoutes = [
  "login",
];

const authHeaderBlackList = [];

const loaderBlackList = [];

export {
  publicRoutes,
  AUTH_URL,
  authHeaderBlackList,
  loaderBlackList
}